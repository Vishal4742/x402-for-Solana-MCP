# Dashboard Placeholder

The infra-heavy stack keeps the request path in Go and the developer-facing client in TypeScript.

Add the operator dashboard after the payment flow is stable. Recommended UI stack:

- `Next.js`
- wallet sign-in only if it does not slow MVP
- receipts, failed payments, and tool pricing as the first three views

